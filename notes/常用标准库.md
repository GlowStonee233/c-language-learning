# C语言常用标准库笔记

## 📚 一、输入输出库 `<stdio.h>`

### 1.1 标准输入输出函数

```c
/* 格式化输入输出 */
int printf(const char *format, ...);        // 格式化输出到stdout
int scanf(const char *format, ...);         // 从stdin格式化读取
int sprintf(char *str, const char *format, ...);    // 格式化输出到字符串
int sscanf(const char *str, const char *format, ...); // 从字符串格式化读取

/* 字符输入输出 */
int getchar(void);          // 从stdin读取一个字符
int putchar(int c);         // 向stdout输出一个字符
int getc(FILE *stream);     // 从文件流读取字符
int putc(int c, FILE *stream); // 向文件流输出字符

/* 字符串输入输出 */
char *gets(char *str);      // 读取一行（已废弃，不安全）
char *fgets(char *str, int n, FILE *stream); // 安全读取一行
int puts(const char *str);  // 输出字符串并换行
```

### 1.2 文件操作函数

```c
/* 文件打开关闭 */
FILE *fopen(const char *filename, const char *mode);
int fclose(FILE *stream);

/* 文件读写 */
size_t fread(void *ptr, size_t size, size_t nmemb, FILE *stream);
size_t fwrite(const void *ptr, size_t size, size_t nmemb, FILE *stream);
int fgetc(FILE *stream);
int fputc(int c, FILE *stream);
char *fgets(char *s, int size, FILE *stream);
int fputs(const char *s, FILE *stream);
int fprintf(FILE *stream, const char *format, ...);
int fscanf(FILE *stream, const char *format, ...);

/* 文件定位 */
int fseek(FILE *stream, long offset, int whence);
long ftell(FILE *stream);
void rewind(FILE *stream);

/* 文件状态 */
int feof(FILE *stream);     // 检查文件结束标志
int ferror(FILE *stream);   // 检查文件错误
void clearerr(FILE *stream); // 清除错误标志
```

### 1.3 格式说明符总结

| 格式符 | 含义 | 示例 |
|--------|------|------|
| `%d` | 有符号十进制整数 | `printf("%d", 123)` |
| `%u` | 无符号十进制整数 | `printf("%u", 123u)` |
| `%o` | 八进制整数 | `printf("%o", 123)` |
| `%x` | 十六进制整数（小写） | `printf("%x", 255)` |
| `%X` | 十六进制整数（大写） | `printf("%X", 255)` |
| `%f` | 浮点数 | `printf("%f", 3.14)` |
| `%e` | 科学计数法（小写） | `printf("%e", 3.14)` |
| `%E` | 科学计数法（大写） | `printf("%E", 3.14)` |
| `%g` | 自动选择`%f`或`%e` | `printf("%g", 3.14)` |
| `%c` | 单个字符 | `printf("%c", 'A')` |
| `%s` | 字符串 | `printf("%s", "hello")` |
| `%p` | 指针地址 | `printf("%p", ptr)` |
| `%%` | 百分号字符 | `printf("%%")` |

## 📝 二、字符串处理库 `<string.h>`

### 2.1 长度与复制

```c
/* 字符串长度 */
size_t strlen(const char *s);  // 返回字符串长度（不包括'\0'）

/* 字符串复制 */
char *strcpy(char *dest, const char *src);  // 复制字符串
char *strncpy(char *dest, const char *src, size_t n);  // 复制前n个字符
char *strdup(const char *s);  // 动态分配复制（POSIX标准）
```

### 2.2 连接与比较

```c
/* 字符串连接 */
char *strcat(char *dest, const char *src);  // 连接字符串
char *strncat(char *dest, const char *src, size_t n);  // 连接前n个字符

/* 字符串比较 */
int strcmp(const char *s1, const char *s2);  // 比较字符串
int strncmp(const char *s1, const char *s2, size_t n);  // 比较前n个字符
int strcasecmp(const char *s1, const char *s2);  // 忽略大小写比较
```

### 2.3 查找与分割

```c
/* 字符查找 */
char *strchr(const char *s, int c);   // 查找字符首次出现
char *strrchr(const char *s, int c);  // 查找字符最后出现

/* 子串查找 */
char *strstr(const char *haystack, const char *needle);  // 查找子串

/* 字符串分割 */
char *strtok(char *str, const char *delim);  // 字符串分割
char *strtok_r(char *str, const char *delim, char **saveptr);  // 线程安全版本
```

### 2.4 内存操作

```c
/* 内存复制与移动 */
void *memcpy(void *dest, const void *src, size_t n);  // 内存复制
void *memmove(void *dest, const void *src, size_t n); // 内存移动（处理重叠）
void *memset(void *s, int c, size_t n);  // 内存设置（常用清零）
int memcmp(const void *s1, const void *s2, size_t n);  // 内存比较
void *memchr(const void *s, int c, size_t n);  // 内存中查找字符
```

## 🔢 三、数学库 `<math.h>`

### 3.1 基本数学函数

```c
/* 幂函数与对数 */
double pow(double x, double y);    // x的y次幂
double sqrt(double x);             // 平方根
double exp(double x);              // e的x次幂
double log(double x);              // 自然对数
double log10(double x);            // 以10为底对数

/* 绝对值与取整 */
double fabs(double x);             // 浮点数绝对值
double ceil(double x);             // 向上取整
double floor(double x);            // 向下取整
double round(double x);            // 四舍五入
double trunc(double x);            // 向零取整

/* 三角函数 */
double sin(double x);              // 正弦
double cos(double x);              // 余弦
double tan(double x);              // 正切
double asin(double x);             // 反正弦
double acos(double x);             // 反余弦
double atan(double x);             // 反正切
double atan2(double y, double x);  // 反正切（两个参数）
```

### 3.2 其他数学函数

```c
double fmod(double x, double y);   // 浮点数取模
double modf(double x, double *intpart);  // 分离整数和小数部分
double frexp(double x, int *exp);  // 分解为尾数和指数
double ldexp(double x, int exp);   // x乘以2的exp次幂
double hypot(double x, double y);  // 计算直角三角形的斜边长度
```

## 🔤 四、字符处理库 `<ctype.h>`

### 4.1 字符分类函数

```c
int isalnum(int c);  // 是否为字母或数字 (A-Z, a-z, 0-9)
int isalpha(int c);  // 是否为字母 (A-Z, a-z)
int iscntrl(int c);  // 是否为控制字符
int isdigit(int c);  // 是否为数字 (0-9)
int isgraph(int c);  // 是否为图形字符（除空格外的可打印字符）
int islower(int c);  // 是否为小写字母
int isprint(int c);  // 是否为可打印字符（包括空格）
int ispunct(int c);  // 是否为标点符号
int isspace(int c);  // 是否为空白字符（空格、制表符、换行等）
int isupper(int c);  // 是否为大写字母
int isxdigit(int c); // 是否为十六进制数字 (0-9, A-F, a-f)
```

### 4.2 字符转换函数

```c
int tolower(int c);  // 转换为小写字母
int toupper(int c);  // 转换为大写字母
```

## 💾 五、动态内存管理 `<stdlib.h>`

### 5.1 内存分配与释放

```c
/* 内存分配 */
void *malloc(size_t size);           // 分配指定字节数的内存（不初始化）
void *calloc(size_t nmemb, size_t size); // 分配并清零内存
void *realloc(void *ptr, size_t size);   // 重新分配内存

/* 内存释放 */
void free(void *ptr);                // 释放内存
```

### 5.2 类型转换函数

```c
/* 字符串转数值 */
int atoi(const char *nptr);          // 字符串转整数
long atol(const char *nptr);         // 字符串转长整数
double atof(const char *nptr);       // 字符串转浮点数

long strtol(const char *nptr, char **endptr, int base);  // 字符串转长整数
unsigned long strtoul(const char *nptr, char **endptr, int base);  // 字符串转无符号长整数
double strtod(const char *nptr, char **endptr);  // 字符串转双精度浮点数
```

### 5.3 随机数函数

```c
int rand(void);            // 生成0到RAND_MAX之间的随机数
void srand(unsigned int seed);  // 设置随机数种子
```

### 5.4 其他实用函数

```c
/* 排序与搜索 */
void qsort(void *base, size_t nmemb, size_t size,
           int (*compar)(const void *, const void *));  // 快速排序
void *bsearch(const void *key, const void *base,
              size_t nmemb, size_t size,
              int (*compar)(const void *, const void *));  // 二分查找

/* 程序控制 */
void exit(int status);     // 正常终止程序
void abort(void);          // 异常终止程序
int system(const char *command);  // 执行系统命令

/* 环境函数 */
char *getenv(const char *name);  // 获取环境变量
```

## ⏰ 六、时间库 `<time.h>`

### 6.1 时间类型和函数

```c
/* 时间类型 */
time_t        // 时间类型（通常为长整型）
struct tm     // 时间结构体
clock_t       // 时钟类型

/* 时间获取 */
time_t time(time_t *tloc);  // 获取当前时间戳
clock_t clock(void);        // 获取程序运行CPU时间

/* 时间转换 */
struct tm *localtime(const time_t *timep);    // 转为本地时间
struct tm *gmtime(const time_t *timep);       // 转为UTC时间
time_t mktime(struct tm *tm);                 // 时间结构体转时间戳

/* 时间格式化 */
char *asctime(const struct tm *tm);           // 时间结构体转字符串
char *ctime(const time_t *timep);             // 时间戳转字符串
size_t strftime(char *s, size_t max, const char *format,
                const struct tm *tm);          // 自定义格式输出

/* 时间计算 */
double difftime(time_t time1, time_t time0);  // 计算时间差
```

### 6.2 struct tm 结构体

```c
struct tm {
    int tm_sec;    // 秒 [0-60]
    int tm_min;    // 分 [0-59]
    int tm_hour;   // 时 [0-23]
    int tm_mday;   // 日 [1-31]
    int tm_mon;    // 月 [0-11]
    int tm_year;   // 年（从1900开始）
    int tm_wday;   // 星期 [0-6]
    int tm_yday;   // 一年中的第几天 [0-365]
    int tm_isdst;  // 夏令时标志
};
```

## 📏 七、类型与常量定义库

### 7.1 `<stddef.h>` 常用定义

```c
/* 常用类型和常量 */
typedef unsigned int size_t;    // 表示对象大小的无符号类型
typedef int ptrdiff_t;          // 指针差值的类型
#define NULL ((void *)0)        // 空指针常量
#define offsetof(type, member)  // 成员在结构体中的偏移量
```

### 7.2 `<limits.h>` 数值极限

```c
/* 字符类型极限 */
CHAR_BIT    // 一个字节的位数（通常为8）
CHAR_MIN    // char类型最小值
CHAR_MAX    // char类型最大值

/* 整数类型极限 */
INT_MAX     // int类型最大值
INT_MIN     // int类型最小值
LONG_MAX    // long类型最大值
LONG_MIN    // long类型最小值
UINT_MAX    // unsigned int类型最大值
ULONG_MAX   // unsigned long类型最大值

/* 其他极限 */
SCHAR_MAX   // signed char最大值
SCHAR_MIN   // signed char最小值
UCHAR_MAX   // unsigned char最大值
SHRT_MAX    // short最大值
SHRT_MIN    // short最小值
USHRT_MAX   // unsigned short最大值
```

### 7.3 `<float.h>` 浮点数特性

```c
/* 浮点数精度 */
FLT_RADIX     // 浮点数基数的指数表示
FLT_MANT_DIG  // float类型尾数位数
DBL_MANT_DIG  // double类型尾数位数
LDBL_MANT_DIG // long double类型尾数位数

/* 浮点数范围 */
FLT_MIN       // float类型最小正数
FLT_MAX       // float类型最大值
DBL_MIN       // double类型最小正数
DBL_MAX       // double类型最大值
LDBL_MIN      // long double类型最小正数
LDBL_MAX      // long double类型最大值

/* 浮点数精度 */
FLT_EPSILON   // float类型精度
DBL_EPSILON   // double类型精度
LDBL_EPSILON  // long double类型精度
```

## ⚠️ 八、断言库 `<assert.h>`

```c
/* 断言宏 */
void assert(int expression);

/* 用法示例 */
#include <assert.h>
#include <stdio.h>

int main() {
    int x = 5;
    assert(x > 0);  // 如果x <= 0，程序终止并输出错误信息
    printf("x is positive\n");
    return 0;
}

/* 禁用断言 */
// 编译时定义NDEBUG宏可禁用所有断言
// gcc -DNDEBUG program.c
```

## 🔧 九、错误处理 `<errno.h>`

```c
/* 错误号 */
extern int errno;  // 全局错误变量

/* 错误代码 */
EPERM     // 操作不允许
ENOENT    // 文件或目录不存在
EINTR     // 系统调用被中断
EIO       // I/O错误
EBADF     // 文件描述符无效
EAGAIN    // 资源暂时不可用
ENOMEM    // 内存不足
EACCES    // 权限拒绝
EFAULT    // 地址错误
EEXIST    // 文件已存在
EINVAL    // 参数无效
EMFILE    // 打开文件过多
ENOSPC    // 设备无剩余空间

/* 错误处理函数 */
void perror(const char *s);  // 打印错误信息
char *strerror(int errnum);  // 获取错误描述字符串
```

## 📋 十、实用函数库 `<stdarg.h>`

```c
/* 可变参数处理 */
typedef va_list;  // 可变参数列表类型

void va_start(va_list ap, last);  // 初始化可变参数列表
type va_arg(va_list ap, type);     // 获取下一个参数
void va_end(va_list ap);           // 清理可变参数列表
void va_copy(va_list dest, va_list src);  // 复制可变参数列表

/* 示例：自定义可变参数函数 */
#include <stdarg.h>
#include <stdio.h>

int sum(int count, ...) {
    int total = 0;
    va_list args;
    va_start(args, count);
    
    for (int i = 0; i < count; i++) {
        total += va_arg(args, int);
    }
    
    va_end(args);
    return total;
}
```

## 📌 十一、使用建议和注意事项

### 11.1 常用组合模式

1. **文件读写模式**
   ```c
   // 安全文件复制
   FILE *src = fopen("input.txt", "r");
   FILE *dest = fopen("output.txt", "w");
   char buffer[1024];
   
   while (fgets(buffer, sizeof(buffer), src) != NULL) {
       fputs(buffer, dest);
   }
   
   fclose(src);
   fclose(dest);
   ```

2. **动态内存管理**
   ```c
   // 安全内存分配
   int *arr = (int *)malloc(10 * sizeof(int));
   if (arr == NULL) {
       perror("malloc failed");
       exit(EXIT_FAILURE);
   }
   
   // 使用内存
   memset(arr, 0, 10 * sizeof(int));
   
   // 释放内存
   free(arr);
   arr = NULL;  // 避免野指针
   ```

### 11.2 常见陷阱

1. **字符串函数缓冲区溢出**
   ```c
   // 错误示例
   char dest[10];
   strcpy(dest, "This string is too long");  // 缓冲区溢出
   
   // 正确示例
   strncpy(dest, "This string is too long", sizeof(dest) - 1);
   dest[sizeof(dest) - 1] = '\0';  // 确保字符串终止
   ```

2. **内存泄漏检测**
   ```c
   // 使用valgrind检测内存泄漏
   // valgrind --leak-check=full ./program
   ```

3. **浮点数比较**
   ```c
   // 错误示例
   if (a == b) { /* 浮点数直接比较可能出错 */ }
   
   // 正确示例
   #include <math.h>
   if (fabs(a - b) < 1e-6) { /* 使用误差范围比较 */ }
   ```

## 📊 十二、库函数速查表

| 功能 | 常用函数 | 所属头文件 |
|------|----------|------------|
| 格式化输出 | printf, fprintf, sprintf | stdio.h |
| 格式化输入 | scanf, fscanf, sscanf | stdio.h |
| 字符串长度 | strlen | string.h |
| 字符串复制 | strcpy, strncpy | string.h |
| 字符串连接 | strcat, strncat | string.h |
| 字符串比较 | strcmp, strncmp | string.h |
| 内存分配 | malloc, calloc, realloc | stdlib.h |
| 内存释放 | free | stdlib.h |
| 数学运算 | sqrt, pow, sin, cos | math.h |
| 字符判断 | isalpha, isdigit, isspace | ctype.h |
| 时间处理 | time, localtime, strftime | time.h |
| 随机数 | rand, srand | stdlib.h |
| 快速排序 | qsort | stdlib.h |
| 二分查找 | bsearch | stdlib.h |

---

**总结提示**：
- 使用库函数前务必包含相应头文件
- 注意检查函数返回值，特别是I/O和内存分配函数
- 理解缓冲区大小限制，防止溢出
- 动态分配的内存必须配对释放
- 字符串函数以`'\0'`为终止符
- 浮点数比较使用误差范围而非直接相等比较